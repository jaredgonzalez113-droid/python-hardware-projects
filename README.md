# Python Hardware Projects

Learning Python for hardware engineering applications.

## sensor_data.py
Reads voltage readings from a CSV file, calculates statistics 
(average, max, min, range), and flags readings above a set threshold.

## live_plot.py
Reads random voltage values made up inside the Arduino IDE program,
and plots them in real time.